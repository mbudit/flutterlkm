import 'package:flutter/material.dart';

class Ambulance extends StatelessWidget {
  const Ambulance({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text("Ambulance Page Underconstruction...")
      ],
    );
  }
}